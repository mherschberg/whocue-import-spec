# WhoCue import-spec has no required local environment variables.
#
# Keep real local values in a gitignored .env file or .secrets/ directory if a
# later STEP introduces local-only tooling configuration.
